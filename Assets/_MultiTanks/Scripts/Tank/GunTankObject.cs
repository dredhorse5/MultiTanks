using System;
using System.Collections;
using System.Collections.Generic;
using Mirror;
using UnityEngine;

/*
public class GunTankObject : NetworkBehaviour
{
    
    public ParticleSystem FireParticles;
    public Transform ProjectileSpawnPlace;

    


    [ClientRpc]
    public void FireFeedback()
    {
        if(isServer)
            owner.AddImpulse(-ProjectileSpawnPlace.transform.forward * ShootForce, ProjectileSpawnPlace.transform.position);
        FireParticles.Play();
    }
}
*/

